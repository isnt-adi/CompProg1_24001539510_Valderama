#include <iostream>
#include <string>
using namespace std;

int main() {
    string asterisk = "*****";
    for (int i = 5; i > 0; i--) {
        cout << asterisk << "\n";
        asterisk = asterisk.substr(0, i - 1);
    }
    return 0;
}
